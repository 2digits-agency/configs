{
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["^test", "build"]
    },
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "build.ts"],
      "outputs": ["./src/nolyfill.json"]
    }
  }
}
