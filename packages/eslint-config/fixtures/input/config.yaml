name: deploy
version: "1.0"
services:
  api:
    port: 3000
    replicas: 3
  database:
    port: 5432
    replicas: 1
config:
  timeout: 30
  retries: 3
  enabled: true
environment:
  - name: NODE_ENV
    value: production
  - name: LOG_LEVEL
    value: info
