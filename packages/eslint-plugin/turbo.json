{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "tsdown.config.ts", "!tests/**", "!vitest.config.ts"]
    },
    "test": {
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.ts"]
    }
  }
}
