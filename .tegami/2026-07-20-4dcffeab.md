---
packages:
  'npm:@2digits/oxlint-config': patch
---

### Relax JavaScript-only lint rules

Legacy `.js` files no longer receive type-aware TypeScript rules or rules that conflict with JSDoc typing and browser null conventions. TypeScript files retain the stricter rules.
