---
packages:
  '@2digits/eslint-config': patch
  '@2digits/eslint-plugin': patch
---

### Reduce ESLint startup and reference lookup costs

- Deferred optional React, CSS and dependency tooling until enabled, including React's Markdown rule disables.
- Skipped package auto-detection for explicit integration options and honored package-local `tsconfigRootDir`.
- Indexed callback references per scope and stopped scanning handlers after finding a second read.
- Documented independently cached, type-aware workspace linting with Turborepo.
