---
'@2digits/eslint-config': minor
'@2digits/oxlint-config': minor
---

Add react-doctor linting configs

- Added `react-doctor` ESLint config with recommended rules to the default `twoDigits()` config
- Added `react-doctor` oxlint config with 150+ rules for React, Next.js, React Native, and TanStack Start
- Updated generated ESLint rule types and factory snapshots for `2digits:react-doctor`
- Relaxed oxlint config `extends` typing to accept typed and raw oxlint configs
