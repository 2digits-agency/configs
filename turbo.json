{
  "pipeline": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist"] },
    "types": { "dependsOn": ["^types"], "cache": false },

    "lint": { "dependsOn": ["^build"] },
    "//#lint:pkg": { "inputs": ["**/package.json"] },
    "//#lint:pkg:fix": { "cache": false },

    "//#format": { "dependsOn": ["^build"] },
    "//#format:fix": { "cache": false }
  }
}
